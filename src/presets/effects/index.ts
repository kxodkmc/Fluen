// Interaction feedback presets (click sparks, ripples, particles, ...).
// Reserved for components whose primary purpose is responding to user input
// with a visual effect.
export * from './ClickSpark';
