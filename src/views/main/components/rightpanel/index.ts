/**
 * RightPanel 模块 — 公共 API。
 *
 * 导出右侧面板容器组件，用于在 Motis 对话与学术助手之间切换。
 *
 * 使用方式：
 * ```ts
 * import { RightPanel } from '@/views/main/components/rightpanel';
 * ```
 */

export { default as RightPanel } from './RightPanel.vue';
