# Fluen 论文标记规范　v1.1

> **定位**：在 Markdown 之上叠加**最少**的自定义标签，只补 Markdown 表达不了的四项能力——
> **编号浮动体、稳定交叉引用、文献库绑定、编号公式/定理**。其余一律用原生 Markdown。
>
> **标签性质**：全部使用 `f-` 前缀，是 W3C **Custom Elements** 合法标签（浏览器可直接识别），
> 不与任何原生 HTML 标签冲突。解析由 Fluen 专用渲染器完成。
>
> **v1.1 变更**：新增 `fallback` 降级属性（§4.3）与完整性校验流程（§8.5），
> 解决外部数据（文献库 / 交叉引用目标）失效时标签无法输出的问题。

---

## 0. 一句话原则

**Markdown 优先，标签按需，失效可降级。**
一块内容只有在需要「编号 / 被引用 / 绑定文献库 / 作为浮动体」时才加标签；
凡依赖外部数据的标签，均应提供 `fallback` 以保证失效时仍可读。

---

## 1. 设计原则

1. **Markdown 优先**：标题、列表、加粗、行内代码、行内公式 `$...$`、无编号块公式 `$$...$$`、
   脚注 `[^1]`、引用块 `>` 全部原生，不加标签。
2. **标签按需**：每个标签必须能用「去掉它就丢失某项论文能力」证明必要性（核验见 §11）。
3. **ID 语义化、稳定**：id 用语义名（`fig:method-overview`），**禁止** `fig:1` 这类会随重排变化的名字；
   显示编号（图 1）由渲染器按出现顺序自动生成，重排自动更新，id 永不变。
4. **受控属性**：枚举属性（如 `type`）取固定值集合，其余取值由 linter 报错，杜绝拼写歧义。
5. **标签内解析规则**：除 `<f-eq>`（仅 LaTeX）与 `<f-tbl>` 形态 C（HTML 表，单元格内容仍二次解析）外，
   所有标签内部内容**按 Markdown 二次解析**，可继续使用 `<f-cite>` / `<f-xref>` / `$...$` / 列表等。
6. **失效可降级**：凡依赖外部数据（文献库、文档内 id）的标签，提供 `fallback` 属性作为后备显示，
   保证数据缺失时正文不出现空洞（详见 §4.3、§8.5）。

---

## 2. 标签速查表

| 形态 | 标签 | 作用 | 关键属性 | 何时使用 |
|------|------|------|----------|----------|
| 行内 | `<f-cite>` | 引用文献库 | `ref` [`loc`] [`fallback`] | 出现引文时 |
| 行内 | `<f-xref>` | 文内交叉引用 | `to` [`fallback`] | 引用图/表/式/节/定理时 |
| 块 | `<f-eq>` | 编号公式 | [`id`] | 公式需编号时 |
| 块 | `<f-fig>` | 图（浮动体） | [`id`] `src` [`alt`] | 插图 |
| 块 | `<f-tbl>` | 表（浮动体） | [`id`] [`src`] [`variant`] | 表格（含合并单元格） |
| 块 | `<f-claim>` | 定理/定义/引理… | [`id`] `type` | 数学/定理类内容 |
| 辅助 | `<f-caption>` | 图/表题注 | — | 仅用于 `<f-fig>`/`<f-tbl>` 内部 |

**共 7 个标签。** 所有 id 均为**可选**（编号始终自动生成；id 仅在被 `<f-xref>` 引用时需要）。

**属性类别说明：**
- `fallback`：仅当目标无法解析时显示的**后备文字**（§4.3）。`<f-cite>` 与 `<f-xref>` 适用。
- `alt`：图片的文字描述（资源失效或无障碍场景），语义同 HTML `<img alt>`，仅 `<f-fig>` 适用。
- 方括号 `[]` 表示可选属性。

---

## 3. ID 与编号约定

### 3.1 ID 前缀与类型的强制映射

id 前缀由元素类型**唯一确定**，linter 据此校验一致性：

| 元素 | id 前缀 | 示例 |
|------|---------|------|
| 图 | `fig:` | `fig:loss-curve` |
| 表 | `tbl:` | `tbl:results` |
| 公式 | `eq:` | `eq:euler-identity` |
| 章节 | `sec:` | `sec:method` |
| 定理类 | 见下表 | `thm:convergence` |

**定理类 `<f-claim>` 的 `type` ↔ id 前缀 ↔ 显示标签映射：**

| `type=""` | id 前缀 | 显示标签（中／英） |
|-----------|---------|--------------------|
| `theorem` | `thm:` | 定理 ／ Theorem |
| `lemma` | `lem:` | 引理 ／ Lemma |
| `definition` | `def:` | 定义 ／ Definition |
| `proposition` | `prop:` | 命题 ／ Proposition |
| `corollary` | `cor:` | 推论 ／ Corollary |
| `example` | `exa:` | 例 ／ Example |
| `remark` | `rem:` | 注 ／ Remark |

> linter 规则：`type="theorem"` 的元素，其 id 必须以 `thm:` 开头，否则报错。

### 3.2 编号流（各自独立、连续）

| 类别 | 编号方式 |
|------|----------|
| 图 / 表 / 公式 | 全文各自独立连续：图 1,2,3…；表 1,2,3…；式 (1),(2),(3)… |
| 定理类 | **每种 type 独立计数**：定理 1,定理 2…；引理 1,引理 2…（可在 front matter 配置为共享单计数流） |
| 章节 | 按 `sections.json` 顺序 + 标题层级生成多级号（如 2.1） |

### 3.3 章节标题的 id

标题本体保持 Markdown；仅当某节需被引用时，加 Pandoc 风格行尾 id（零 HTML）：

```markdown
## 方法 {#sec:method}
```

---

## 4. 行内标签

### 4.1 `<f-cite>` — 文献引用

```html
自监督学习<f-cite ref="ref-a1b2c3d4"/>在近年来取得显著进展，
多重引用用逗号合并<f-cite ref="ref-aaa11111,ref-bbb22222"/>。
带定位<f-cite ref="ref-x" loc="p. 42"/>。
带降级后备<f-cite ref="ref-y" fallback="Smith et al., 2020"/>。
```

- `ref`：文献 id（来自 `references/references.json`），**多个用逗号分隔**（渲染时合并为 `[1,2,3]`）。
- `loc`（可选）：页/节定位，原样透传，渲染如 `[1, p. 42]`。
- `fallback`（可选）：当 `ref` 无法在文献库命中时显示的后备文字（见 §4.3）。
- **默认自闭合**：正常显示文字（`[1]` 或 `Smith, 2020`）由渲染器按引用风格生成，不手写。
- **相邻的多个独立 `<f-cite>` 不自动合并**；要合并须用逗号语法（语义明确）。

### 4.2 `<f-xref>` — 交叉引用

```html
如<f-xref to="fig:overview"/>所示，本方法在<f-xref to="tbl:results"/>上
优于基线；推导见<f-xref to="eq:loss"/>，证明见<f-xref to="thm:convergence"/>。
带降级后备<f-xref to="fig:old" fallback="图（旧版损失曲线）"/>。
```

- `to`：目标元素 id（图/表/式/定理/节均可）。
- `fallback`（可选）：当 `to` 目标不存在时显示的后备文字（见 §4.3）。
- 渲染器按目标 id 前缀**自动生成前缀文字**：

  | 目标前缀 | 生成文字 |
  |----------|----------|
  | `fig:` | 图 N |
  | `tbl:` | 表 N |
  | `eq:` | 式 (N) |
  | `sec:` | 第 N.M 节 |
  | `thm:`/`lem:`/… | 对应类型标签 + N（如「定理 1」） |

- 默认自闭合。手写内部文字可覆盖（如 `<f-xref to="fig:overview">该图</f-xref>`），但会失去自动更新，一般不推荐。

### 4.3 `fallback` — 降级显示机制（v1.1 核心）

**解决的问题**：`<f-cite>`、`<f-xref>` 的正常显示完全依赖外部数据
（文献库条目 / 文档内 id）。当数据因删除、损坏、重命名、索引失败而无法解析时，
自闭合标签若无后备，正文将出现空洞。`fallback` 提供一份**可读快照**兜底。

**语义（与"覆盖显示"严格区分）：**

| 写法 | 含义 | 何时显示内部/属性文字 |
|------|------|------------------------|
| `<f-cite ref="x" fallback="Smith, 2020"/>` | **降级后备** | **仅当** ref 命中失败时显示 `Smith, 2020` |
| `<f-cite ref="x">Smith</f-cite>` | **覆盖显示** | **始终**显示「Smith」，忽略文献库（不推荐） |

> 一句话：`fallback` 是"备胎"，正常时不出现；内部内容是"替身"，始终顶替。
> 二者互斥，**同一标签不应同时使用**（linter 报错）。

**fallback 推荐格式：**
- `<f-cite>`：作者-年（`Smith et al., 2020`）或标题缩写——即该文献的标准引用显示形式。
- `<f-xref>`：类型 + 名（`图（损失曲线）`、`式（欧拉恒等式）`、`定理（收敛性）`）。

**渲染标记：** 降级显示时须加视觉提示，让作者察觉并修复（避免错误无声潜伏）：
- 文本预览：`[Smith, 2020 ⚠]`、`图（损失曲线）⚠`
- HTML：降级文字加警告色 / 虚下划线 / `title` 悬停说明失效原因
- 导出（LaTeX/Word）：保留 `⚠` 或转为编辑批注

**一致性策略（不强制同步）：**
- `fallback` 是写入时的快照，**默认不要求**与文献库/id 实时一致——
  否则它退化成必须维护的冗余数据，失去"降级后备"意义。
- linter **宽松模式（默认）**：仅在目标失效时启用 fallback 并告警。
- linter **严格模式（可选）**：目标正常时，若 fallback 与实际解析值不一致则告警，
  提示更新（适用于定稿前清扫）。

### 4.4 `alt` — 图片描述（仅 `<f-fig>`）

`<f-fig src="assets/a.png" alt="损失曲线：训练 100 轮后趋于平稳。">`
语义同 HTML `<img alt>`：图片加载失败或无障碍场景下显示。与 `fallback`（交叉引用后备）
是不同维度，不混用。

---

## 5. 块级标签

> **块级标签必须独占一行**（前后空行），避免与 Markdown 段落拼接产生歧义。

### 5.1 `<f-eq>` — 编号公式

```html
<f-eq id="eq:euler">
e^{i\pi} + 1 = 0
</f-eq>
```

- 内容为 **LaTeX 源码**（不写 `$` 定界符；前后空白自动去除）。
- **始终自动编号**；`id` 可选（仅在被引用时需要）。
- 内容按**原始文本**处理（不二次解析 Markdown、不解码 HTML 实体）；
  含 `<` `>` 时用 LaTeX 写法 `\lt` `\gt`。
- **不需编号的公式一律用原生 Markdown**，不用标签：行内 `$a^2+b^2$`，块级 `$$\int_0^1 x\,dx$$`。

### 5.2 `<f-fig>` — 图

```html
<f-fig id="fig:overview" src="assets/overview.svg" alt="系统总览示意图。">
  <f-caption>系统总览。数据流见<f-xref to="fig:dataflow"/>。</f-caption>
</f-fig>
```

- `src`：图片路径，**相对 `manuscript/` 内容根**（即 `manuscript/assets/overview.svg`）。
- `alt`（可选）：图片文字描述（§4.4）。
- 内部**有且仅有一个** `<f-caption>`；`<f-caption>` 内可嵌 `<f-cite>`/`<f-xref>`/`$...$`。

### 5.3 `<f-tbl>` — 表

`<f-tbl>` 的表体有**三种互斥形态**，按需选用（复杂度递增）：

**形态 A — 外部数据源（推荐，便于复用与导出）：**

```html
<f-tbl id="tbl:results" src="assets/results.csv">
  <f-caption>各数据集准确率对比。</f-caption>
</f-tbl>
```

**形态 B — 内嵌 Markdown 表（简单表，无合并）：**

```html
<f-tbl id="tbl:results">
  <f-caption>各数据集准确率对比。</f-caption>

  | 方法 | Acc |
  |------|-----|
  | Ours | 95% |
</f-tbl>
```

**形态 C — 内嵌 HTML `<table>` 子集（需要合并单元格等复杂结构）：**

```html
<f-tbl id="tbl:ablation">
  <f-caption>消融实验。「方法」列跨两行。</f-caption>

  <table>
    <thead>
      <tr><th>方法</th><th>精度</th><th>召回</th></tr>
    </thead>
    <tbody>
      <tr><td rowspan="2">Ours</td><td>95</td><td>90</td></tr>
      <tr><td>93</td><td>88</td></tr>
      <tr><td>Baseline</td><td>80</td><td>75</td></tr>
    </tbody>
  </table>
</f-tbl>
```

**规则：**

- 三种形态**互斥**：`src` 属性、内嵌 MD 表、内嵌 HTML `<table>`，只能存在其一。
- `src` 指向 CSV / JSON（相对 `manuscript/` 根），渲染器自动建表。
- 形态 C 的 **HTML 子集限定**为：`<table>`/`<thead>`/`<tbody>`/`<tr>`/`<th>`/`<td>`，
  属性仅允许 `colspan`/`rowspan`（只在 `<th>`/`<td>` 上）。其余标签或属性 linter 报错。
- 形态 C 的**单元格内容仍按 Markdown 二次解析**（可写 `$...$`、`<f-cite>` 等），
  与其他标签内规则一致；这是 `<f-tbl>` 的特例声明。
- 图与表**各自独立编号流**。

#### 三线表（渲染默认风格，非标签）

三线表是学术表格标准样式，由**渲染层默认输出**，不需要任何标签或属性：

- 顶线（粗）+ 表头与表体分隔线（细）+ 底线（粗）；
- 无竖线、无表体内横线。

表头信息从数据天然获取：MD 表的 `|---|` 分隔行、HTML 表的 `<thead>`、
或 CSV 的首行——渲染器据此判定表头边界。因此**三线表对三种形态统一生效，零额外标记**。
若个别表需切换为网格表等其他样式，预留可选属性 `variant="grid"`（默认 `variant="threeline"`，
通常无需显式写出）。

### 5.4 `<f-claim>` — 定理 / 定义 / 引理…

```html
<f-claim id="thm:convergence" type="theorem">
设损失函数 $L$ 满足 Lipschitz 连续，则算法以 $O(1/\sqrt{t})$ 速率收敛。
</f-claim>
```

- `type`：受控枚举（见 §3.1），取其他值 linter 报错。
- 内容为 Markdown，可含 `<f-cite>`/`<f-xref>`/`$...$`/列表/多段。
- **证明不引入额外标签**，用普通段落 + 加粗前缀：

  ```markdown
  <f-claim id="thm:conv" type="theorem">…</f-claim>

  ***证明.*** 由<f-xref to="lem:aux"/>直接可得。　□
  ```

---

## 6. 嵌套与校验规则

### 6.1 允许的嵌套

| 容器 | 允许的内部内容 |
|------|----------------|
| `<f-caption>` | `<f-cite>` / `<f-xref>` / `$...$` / 纯文本 |
| `<f-claim>` | `<f-cite>` / `<f-xref>` / `$...$` / 列表 / 段落 |
| `<f-fig>` | 有且仅有一个 `<f-caption>` |
| `<f-tbl>` | 有且仅有一个 `<f-caption>` +（`src` 属性 ｜ 内嵌 MD 表 ｜ 内嵌 HTML `<table>`）之一 |
| `<f-eq>` | 仅 LaTeX，**禁止**任何标签 |
| `<f-cite>`/`<f-xref>` | 不嵌套（行内叶子） |

### 6.2 Linter 强制规则

- `<f-xref to>` 的目标 id 必须存在于全文（组装后）；不存在时启用 `fallback` 显示并告警，
  **无 fallback 则报错**（避免正文空洞）。
- 全文 id **唯一**，重复报错。
- `<f-cite ref>` 的每个文献 id 必须能在 `references.json` 命中；未命中时启用 `fallback` 显示并告警，
  **无 fallback 则报错**。
- `<f-claim type>` 与其 id 前缀必须满足 §3.1 映射，不一致报错。
- `<f-fig>`/`<f-tbl>` 内 `<f-caption>` 数量必须恰为 1。
- `<f-tbl>` 的 `src`、内嵌 MD 表、内嵌 HTML `<table>` 三者互斥，不能共存。
- `<f-tbl>` 内嵌 HTML 表的标签子集限定为 `<table>`/`<thead>`/`<tbody>`/`<tr>`/`<th>`/`<td>`，
  属性仅允许 `colspan`/`rowspan`（在 `<th>`/`<td>` 上），其余报错。
- `<f-tbl variant>` 取值限定为 `threeline`（默认）/`grid`，其余报错。
- `fallback` 与内部覆盖内容**不可同时**出现在同一标签，否则报错。
- 块级标签须独占行（前后为空行或行首/行尾）。

---

## 7. 与项目结构的衔接

| 标记 | 指向 |
|------|------|
| `<f-cite ref>` | `references/references.json` 条目 |
| `<f-fig src>` / `<f-tbl src>` | `manuscript/assets/`（相对 `manuscript/` 根） |
| `## … {#sec:…}` | 章节内标题，供跨章节 `<f-xref>` 引用 |
| 章节顺序 | `manuscript/sections/sections.json` 决定，**不靠标签** |

---

## 8. 核心流程

### 8.1 文献导出
1. 扫描全部章节，收集所有 `<f-cite ref>` 的 id 及首次出现顺序。
2. 按序在 `references.json` 解析元数据，生成**仅含被引文献**的参考文献表。
3. 按风格渲染：数字制 `[1]` ／ 作者-年 `Smith, 2020` ／ 多重合并 `[1,2,3]`。
4. 导出：BibTeX（LaTeX）／ CSL-JSON（Word/Pandoc）／ 回写 `references.json` 摘要字段。

### 8.2 大纲生成
- 一级结构：`sections.json` + 各章节 front matter 标题。
- 二级及以下：解析章节内 `#`/`##`/`###`。
- 附属目录：从 `<f-fig>`/`<f-tbl>`/`<f-eq id>`/`<f-claim id>` 自动生成图/表/式/定理目录。
- 每个节点天然可挂 `<f-xref to>`，点击跳转。

### 8.3 拼接 / 组装
1. 按 `sections.json` 顺序拼接各 `sec-*.md` 成完整文稿。
2. **拼接后统一收集全部 id，一次性重排编号**（图/表/式/各 claim 类型各自从 1 连续），
   保证跨章节 `<f-xref>` 编号正确。
3. 复用通过拆分子章节实现，**默认不引入 `<f-include>`**，避免循环依赖。

### 8.4 交叉引用解析
1. 收集全文 id（`<f-eq>`/`<f-fig>`/`<f-tbl>`/`<f-claim>` 及 `{#sec:…}`）。
2. 为每个 `<f-xref to>` 查表，注入「类型前缀 + 自动编号」。
3. 目标缺失 → 启用 `fallback` + 降级标记；id 重复 → 报错。

### 8.5 完整性校验与降级（v1.1 新增）

**触发时机**：保存时增量校验、导出/渲染前全量校验、用户手动触发。

**校验对象与降级策略：**

| 对象 | 失效场景 | 有 `fallback` | 无 `fallback` |
|------|----------|---------------|----------------|
| `<f-cite ref>` | 文献被删 / JSON 损坏 / id 失配 | 显示 fallback + `⚠` + 收入问题清单 | **报错**（正文将空洞） |
| `<f-xref to>` | 目标元素被删 / 重命名 | 显示 fallback + `⚠` + 收入问题清单 | **报错** |
| `<f-fig src>` | 图片文件缺失 | 显示 `alt`（若有）+ 占位框 + 告警 | 显示占位框 + 告警 |
| `<f-tbl src>` | 数据文件缺失 | 保留题注 + 空表 + 告警 | 同左 |

**问题清单输出**：每次校验生成报告，逐条列出
「文件:行号 → 标签 → 失效原因 → 当前处理（降级/报错）→ 建议动作（恢复数据/补 fallback/删除引用）」。

**一致性校验（严格模式，可选）：** 目标正常但 `fallback` 与实际解析值不一致时告警，
提示更新快照——用于定稿前清扫，避免 fallback 长期偏离。

**数据自愈建议**（非规范强制，属实现层）：
- 文献库写入采用原子替换（写临时文件后 rename），降低损坏概率。
- 关键引用关系可在 `references.json` 额外缓存「被哪些章节引用」的反向索引，
  便于文献被删时定位受影响位置。

---

## 9. 完整示例

```markdown
## 引言 {#sec:intro}

近年来，<f-cite ref="ref-1111aaaa" fallback="Chen et al., 2020"/>提出的对比学习方法
在表征学习领域表现突出（见<f-xref to="fig:framework" fallback="图（整体框架）"/>）。
其核心目标如<f-xref to="eq:contrast"/>所示：

<f-eq id="eq:contrast">
\mathcal{L} = -\log \frac{\exp(z_i\cdot z_+/\tau)}{\sum_j \exp(z_i\cdot z_j/\tau)}
</f-eq>

我们给出收敛性结论：

<f-claim id="thm:conv" type="theorem">
在温度参数 $\tau\in(0,1)$ 且批量足够大时，上述损失以 $O(1/n)$ 速率收敛。
</f-claim>

***证明.*** 由辅助引理（略）直接可得。　□

<f-fig id="fig:framework" src="assets/framework.png" alt="整体框架：编码器-投影头-对比层。">
  <f-caption>整体框架。实验设置见<f-xref to="tbl:setup"/>。</f-caption>
</f-fig>

<f-tbl id="tbl:setup" src="assets/setup.csv">
  <f-caption>主要超参数设置。</f-caption>
</f-tbl>
```

**正常渲染示意**（数据完好）：

> **1 引言**
> 近年来，[1] 提出的对比学习方法……表现突出（见图 1）。其核心目标如式 (1) 所示：
> 　　(1)　ℒ = −log( exp(zᵢ·z₊/τ) / Σⱼ exp(zᵢ·zⱼ/τ) )
> 　　**定理 1.** 在温度参数 τ∈(0,1) 且批量足够大时，上述损失以 O(1/n) 速率收敛。
> 　　*证明.* 由辅助引理（略）直接可得。 □
> 　　**图 1**　整体框架。实验设置见表 1。
> 　　**表 1**　主要超参数设置。

**降级渲染示意**（假设 `ref-1111aaaa` 文献被删、`fig:framework` 目标被改名）：

> 近年来，[Chen et al., 2020 ⚠] 提出的对比学习方法……表现突出（见图（整体框架）⚠）。
> 其核心目标如式 (1) 所示：……

---

## 10. 导出目标映射

| 目标 | 转换 |
|------|------|
| **HTML 预览** | `<f-fig>`→`<figure>`、`<f-tbl>`→`<table>`、`<f-xref>`→`<a href="#id">`、`<f-cite>`→`<sup>`；降级时 `<sup>` 内放 fallback + 警告样式 |
| **LaTeX / Pandoc** | `<f-cite>`→`\cite{}`、`<f-eq>`→`equation`、`<f-fig>`→`figure`、`<f-tbl>`→`table`、`<f-claim>`→`amsthm` 环境、`<f-xref>`→`\ref{}`；HTML 形态 C 的 `colspan`/`rowspan`→`\multicolumn`/`\multirow`，`variant="threeline"`→`\booktabs`；降级时 fallback 作为注释或 `\todo{}` 标注 |
| **Word（docx）** | 经 Pandoc；编号用交叉引用域，图/表用「题注」样式，合并单元格直通；降级项转为批注 |
| **干净 Markdown** | `<f-cite>`→`[1]`、`<f-xref>`→「图 1」；`<f-tbl>` 形态 A/B→MD 表，形态 C（含合并）无法降级为 MD 表，保留为 HTML 或降级为纯文本；失效项保留 fallback 文本 + `⚠` |

---

## 11. 设计自洽性核验

### 11.1 每个标签的必要性（去掉即丢能力）

| 标签 | 若去掉，丢失什么 |
|------|------------------|
| `<f-cite>` | 无法绑定文献库、无法按风格重排、无法导出 BibTeX |
| `<f-xref>` | 链接文字写死，重排后编号错乱；无法自动生成「图 N/式 (N)」 |
| `<f-eq>` | `$$` 无法编号、无法被引用 |
| `<f-fig>` / `<f-tbl>` | 无自动编号、无稳定题注绑定、无独立浮动编号流 |
| `<f-claim>` | 无定理环境与自动编号 |
| `<f-caption>` | 题注须与浮动体稳定绑定，否则编号/交叉引用失锚 |

→ 此为当前**最小完备集**（7 个）。`fallback`/`alt` 是属性不是标签，
解决"标签存在但数据失效"的健壮性问题，不改变标签集合。

### 11.2 无歧义性

- 全部 `f-` 前缀 → 不与原生 HTML 冲突，且为合法 Custom Elements。
- `type` 枚举 + id 前缀强制映射 → 拼写错误 linter 即报。
- `<f-cite>`（外部文献）与 `<f-xref>`（内部交叉引用）职责互斥，边界清晰。
- `<f-tbl>` 的 `src` / 内嵌 MD 表 / 内嵌 HTML `<table>` 三者互斥 → 数据来源唯一。
- 合并单元格仅在 HTML `<table>` 形态下用标准 `colspan`/`rowspan` 表达 → 无需新增标签或属性。
- 三线表是渲染默认样式（`variant` 默认 `threeline`），不增加书写负担。
- `fallback`（降级后备，仅失效时显示）与内部覆盖内容（始终显示）语义互斥，不可同标签共存。

### 11.3 可维护 / 可拓展

- 新增能力遵循同一模式：`f-<name>` + 「去掉即丢能力」原则。
- 预留示例：算法块 `<f-claim type="algorithm">` 复用现有 claim 机制即可编号；
  若需独立编号流与伪代码语法，再新增 `<f-alg>`，不影响既有标签。
- 编号流、引用风格、linter 严格/宽松模式通过 front matter 配置，不侵入标签语法。
- `fallback` 是可选快照属性：不写也能工作（失效时报错），写了增强健壮性，向后兼容。

### 11.4 论文场景覆盖核对

| 场景 | 支持方式 |
|------|----------|
| 文献引用与导出 | `<f-cite>` + §8.1 |
| 自动编号 | fig/tbl/eq/claim 各自连续（§3.2） |
| 交叉引用 | `<f-xref>` 自动前缀（§4.2） |
| 公式（编号/无编号） | `<f-eq>` / `$…$` `$$…$$` |
| 定理环境 | `<f-claim type=…>` |
| 图表浮动 + 题注 | `<f-fig>`/`<f-tbl>` + `<f-caption>` |
| 复杂表格（合并单元格） | `<f-tbl>` 内嵌 HTML `<table>` 子集 + `colspan`/`rowspan`（§5.3 形态 C） |
| 三线表 / 表格样式 | 渲染默认 `variant="threeline"`，无需标记（§5.3） |
| 大纲 / 目录 | §8.2 |
| 多章节拼接 | §8.3 |
| 多目标导出 | §10 |
| **数据失效容错** | `fallback` 降级（§4.3）+ 完整性校验（§8.5） |

---

## 12. 版本与修订

| 版本 | 要点 |
|------|------|
| v1.0 | 7 个 `f-` 标签基线；ID 映射、编号流、合并单元格形态 C、三线表默认 |
| v1.1 | 新增 `fallback` 降级属性（`<f-cite>`/`<f-xref>`）、`alt`（`<f-fig>`）；新增 §4.3 降级机制、§8.5 完整性校验流程 |

> 后续修订须同步更新版本号与 §11 核验表。
